# sensitive-data-exfiltration-detection
Sensitive Data Exfiltration Detection using Suricata IDS demonstrates simulation and detection of outbound data leakage attacks in a virtual lab environment using Ubuntu as the victim machine and Kali Linux as the attacker machine. The project includes custom Suricata IDS rules for detecting HTTP POST exfiltration,etc.
